РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ
автоматизированной системы «Практика»

Дата проведения: 1 июня 2026 г.

Проект: АС «Система управления практиками студентов»

Источники:
- `ТЗ.docx`;
- `ПЗ.docx`;
- папка `tests` проекта;
- `phpunit.xml`.

Тестовая среда:
- PHP 8.5.4;
- PHPUnit 12.5.24;
- Laravel тестовая среда с SQLite `:memory:`;
- Windows 10/11 с PowerShell.

Итоговое выполнение:
- всего тестов: 25;
- всего утверждений: 70;
- статус: OK;
- все тесты пройдены.

Список проверенных направлений:
- доступность публичных страниц (`PublicRoutesTest`);
- работа API-списка студентов и отображение данных (`StudentApiTest`);
- сохранение и чтение JSON-качеств студента (`StudentQualitiesTest`);
- проверка контрактов и условий создания практики (`StudentInternshipContractTest`);
- бронирование практики, отмена бронирования и уведомления (`ReservationBookingTest`);
- создание запросов на договоры и синхронизация контрактов (`ContractRequestControllerTest`);
- загрузка документов и сохранение файлов (`DocumentUploadTest`, `DocumentUploadProfileTest`);
- удаление документов и очистка хранилища (`DocumentDeleteTest`);
- CRUD-операции практик и JSON-ответы (`InternshipCrudTest`);
- профиль компании, отображение вакансий и бронирований (`CompanyProfileDataTest`);
- логика модели студента (`StudentModelTest`).

Покрытие требований:
- учет студентов, компаний, направлений и практик;
- бронирование студентов компаниями и проверка контрактной зависимости;
- контроль распределения студентов и предотвращение дублирования;
- генерация и сохранение документов;
- уведомления исполнителям при бронировании, отмене и назначении;
- проверка прав доступа и отображения данных для разных ролей.

Выводы:
- системная функциональность, реализованная в проекте, подтверждена автоматизированными тестами;
- конкретные бизнес-сценарии из ТЗ/ПЗ зафиксированы и проверены;
- дефектов по выполненным тестам не выявлено.

Рекомендации:
- провести дополнительные приемочные сценарии вручную для проверки пользовательского интерфейса;
- добавить нагрузочные проверки для подтверждения требований времени отклика и одновременной нагрузки;
- фиксировать результаты последующих запусков в рамках версии проекта.
